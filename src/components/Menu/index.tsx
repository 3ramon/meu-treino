import "./style.css";

type cardProps = {
    imageFull: string;
    name: string;
    title: string;
};

export function Menu({ imageFull, name, title }: cardProps) {

    
    return (
        <div className="cardContainerLol">
          Menu
        </div>
    );
}
